export default function Logo() {
  return (
    <div 
      style={{
        fontFamily: "'Antic', sans-serif",
        fontSize: "10vw",
        letterSpacing: "-3px",
        wordSpacing: "-15px",
        padding: "15px",
        cursor: "default"
      }}
    >
      <span style={{color: "gray"}}>Simple </span>
      <span style={{color: "coral"}}>Auth</span>
    </div>
  )
}